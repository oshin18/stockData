
/** ********************** Require Local modules ********************* */
const authentication = require('./authentication');
const encryption = require('./encryption');

module.exports = {
  authentication,
  encryption,
};
